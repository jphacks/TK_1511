<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tk11.jphacks.titech">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.Base">
        <activity android:name=".view.activity.SplashActivity_">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.activity.HomeActivity_"
            android:theme="@style/RevealActivityTheme">
            </activity>
        <activity android:name=".view.activity.CallActivity_"
            android:theme="@style/RevealActivityTheme"/>
        <activity android:name=".view.activity.BindingActivity_"
            android:theme="@style/RevealActivityTheme"/>
        <activity android:name=".view.activity.SettingActivity_"
            android:theme="@style/RevealActivityTheme"></activity>
    </application>

</manifest>
